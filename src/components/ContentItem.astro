---
interface Props {
  title: string
  subtitle: string
  period: string
  description: string
  href?: string
}

const { title, subtitle, period, description, href } = Astro.props
---

<div class="border-b border-border py-6 first:pt-0 last:border-0 last:pb-0">
  <div
    class="flex flex-col gap-2 sm:flex-row sm:items-baseline sm:justify-between sm:gap-4"
  >
    <div class="flex-1">
      {
        href ? (
          <a
            href={href}
            target="_blank"
            rel="noopener noreferrer"
            class="group inline-block"
          >
            <p class="font-sans text-base font-medium text-text transition-colors duration-150 group-hover:text-text-muted">
              {title}
            </p>
          </a>
        ) : (
          <p class="font-sans text-base font-medium text-text">
            {title}
          </p>
        )
      }
      <p
        class:list={[
          "font-sans text-sm text-text-muted",
          href ? "mt-1" : "mt-0.5",
        ]}
      >
        {subtitle}
      </p>
    </div>
    <p
      class="shrink-0 font-sans text-sm text-text-muted tabular-nums"
    >
      {period}
    </p>
  </div>
  <p class="mt-3 text-sm leading-relaxed text-text-muted">
    {description}
  </p>
</div>

---
interface Props {
  title: string
  href: string
  date: Date
  description: string
}

const { title, href, date, description } = Astro.props

const year = date.toLocaleDateString("en-US", {
  timeZone: "UTC",
  year: "numeric",
})
---

<a href={href} class="item">
  <h3><span class="link-underline">{title}</span></h3>
  <p class="meta">
    writing <time datetime={date.toISOString()}>{year}</time>
  </p>
  {description && <p class="description">{description}</p>}
</a>

<style>
  .item {
    display: block;
    padding-block: var(--spacing-fluid-l);
    border-bottom: 1px solid var(--color-border-subtle);
  }

  .item:first-child {
    padding-top: 0;
  }

  .item:last-child {
    border-bottom: none;
    padding-bottom: 0;
  }

  h3 {
    font-size: var(--text-fluid-lg);
    line-height: var(--line-height-tight);
    letter-spacing: var(--tracking-tight);
    font-weight: var(--font-weight-semibold);
    color: var(--color-text);
  }

  .item:hover .link-underline,
  .item:focus-visible .link-underline {
    background-size: 100% 1px;
  }

  .meta {
    color: var(--color-text-muted);
    margin-top: 0.25rem;
    font-size: var(--text-fluid-xs);
  }

  .description {
    color: var(--color-text);
    margin-top: var(--spacing-fluid-s);
    max-width: 55ch;
  }
</style>

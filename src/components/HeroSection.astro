---

---

<section class="hero">
  <h1 id="hero-text">
    I craft meaningful digital experiences with <em>care</em>, <em>clarity</em> and
    <em>intention</em> &mdash; helping people leave things better than they found
    them.
  </h1>
  <p class="intro">
    I'm an Italian engineer living in Paris, building things slowly and on
    purpose. This is where I write, think out loud, and leave traces of the
    work.
  </p>
</section>

<style>
  .hero {
    margin-bottom: var(--spacing-fluid-3xl);

    & h1 {
      font-size: var(--text-fluid-3xl);
      line-height: var(--line-height-display);
      letter-spacing: var(--tracking-display);
      font-weight: var(--font-weight-semibold);
      text-wrap: balance;
    }
  }

  .intro {
    margin-top: var(--spacing-fluid-2xl);
    font-size: var(--text-fluid-base);
    line-height: var(--line-height-body);
    color: var(--color-text-muted);
    max-width: var(--measure-narrow);
  }
</style>

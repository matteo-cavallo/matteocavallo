---

---

<div class="banner">
  <p>
    <strong>D'ici</strong> — French for <em>from here</em> — is a newsletter about
    technology, generational change, and what it means to be human in an era that
    keeps asking us to be faster.
  </p>
  <a href="#" class="cta">Subscribe →</a>
</div>

<style>
  .banner {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-fluid-m);
    border: 1px solid var(--color-border);
    border-radius: 4px;
    padding: var(--spacing-fluid-l);
  }

  .banner p {
    font-size: var(--text-fluid-xs);
    line-height: var(--line-height-body);
    color: var(--color-text-muted);

    & em {
      font-family: var(--font-sans);
    }
  }

  .banner strong {
    font-weight: var(--font-weight-semibold);
    color: var(--color-text);
  }

  .cta {
    font-size: var(--text-fluid-xs);
    color: var(--color-text);
    text-decoration: underline;
    text-decoration-color: var(--color-gray-7);
    text-underline-offset: max(0.15em, 3px);
    transition: text-decoration-color 200ms ease;
    align-self: flex-start;

    @media (hover: hover) {
      &:hover {
        text-decoration-color: var(--color-text);
      }
    }
  }
</style>
